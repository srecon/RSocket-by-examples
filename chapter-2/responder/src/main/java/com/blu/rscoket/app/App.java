/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.blu.rscoket.app;

import com.blu.rscoket.list.LinkedList;

import static com.blu.rscoket.utilities.StringUtils.join;
import static com.blu.rscoket.utilities.StringUtils.split;
import static com.blu.rscoket.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
