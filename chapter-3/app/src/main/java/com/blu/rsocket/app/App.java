/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.blu.rsocket.app;

import com.blu.rsocket.list.LinkedList;

import static com.blu.rsocket.utilities.StringUtils.join;
import static com.blu.rsocket.utilities.StringUtils.split;
import static com.blu.rsocket.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
